<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Berechtigungen für Hintergrund-Alarm -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM entfernt: die App nutzt gar
         keinen AlarmManager (nur CountDownTimer im Service), diese beiden
         Berechtigungen wurden nie gebraucht. -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.IntervalAlarm"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
          android:name=".EditReminderActivity"
          android:exported="false"
          android:parentActivityName=".MainActivity" />

        <!-- WICHTIG: Foreground Service für Hintergrund-Betrieb -->
        <service
            android:name=".AlarmForegroundService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Boot-Empfänger: startet Alarm nach Neustart neu -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- AlarmReceiver wurde entfernt: er war registriert, wurde aber nie
             von irgendwo im Code ausgelöst (kein AlarmManager im Projekt) –
             toter Code. Die Datei AlarmReceiver.kt kann gelöscht werden. -->

    </application>
</manifest>
